import tokens from "@gemeente-rotterdam/design-tokens/dist/tokens.json";
import { ColorItem, ColorPalette, Markdown, Meta } from "@storybook/blocks";
import markdown from "./color.md?raw";
import { FigmaLink } from "../../config/FigmaBlock";

export const baseColors = tokens["rods"]["color"]["base"];
export const grayTones = tokens["rods"]["color"]["gray"];
export const notificationColors = Object.entries(tokens["rods"]["color"]["notification"]);
export const skinColors = Object.entries(tokens["rods"]["color"]["skin"] || {});
export const extendedColors = Object.entries(tokens["rods"]["color"]).filter(
  ([key]) => !["base", "gray", "notification", "skin"].includes(key),
);

<Meta title="Rotterdam/Color" />

<Markdown>{markdown}</Markdown>

<FigmaLink href="https://www.figma.com/design/RiVsTfcbmnKSU2BGqQBI9n/RODS-fundament?node-id=1-254&t=QH8m3AiVV8aa5Esk-4" />

<ColorPalette>
  {Object.entries(baseColors).map(([key, token]) => (
    <ColorItem title={key} colors={[token]} />
  ))}
</ColorPalette>

## Grays

<ColorPalette>
  <ColorItem title="Gray" colors={Object.values(grayTones)} />
</ColorPalette>

## Notification colors

<FigmaLink href="https://www.figma.com/design/RiVsTfcbmnKSU2BGqQBI9n/RODS-fundament?node-id=1-375&t=QH8m3AiVV8aa5Esk-4" />

<ColorPalette>
  {notificationColors.map(([name, tokens]) => (
    <ColorItem title={name} colors={Object.values(tokens)} />
  ))}
</ColorPalette>

## Colors for illustrations and infographics

<FigmaLink href="https://www.figma.com/design/RiVsTfcbmnKSU2BGqQBI9n/RODS-fundament?node-id=1-290&t=QH8m3AiVV8aa5Esk-4" />

<ColorPalette>
  {extendedColors.map(([name, tokens]) => (
    <ColorItem title={name} colors={Object.values(tokens)} />
  ))}
</ColorPalette>

## Colors for skin tones

<FigmaLink href="https://www.figma.com/file/ZWSC4gCrOXRUR9UX3aoZ8x/?node-id=28801-7125" /> *

<ColorPalette>
  {skinColors.map(([name, tokens]) => (
    <ColorItem title={name} colors={Object.values(tokens)} />
  ))}
</ColorPalette>
